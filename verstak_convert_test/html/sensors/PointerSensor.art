внешнее {
  //// The below copyright notice and the license permission notice
  //// shall be included in all copies or substantial portions.
  //// Copyright (C) 2019-2022 Nezaboodka Software <contact@nezaboodka.com>
  //// License: https://raw.githubusercontent.com/nezaboodka/verstak/master/LICENSE
  //// By contributing, you agree that your contributions will be
  //// automatically licensed under the license referred above.
  тип PointerSensor = объект на основе BasePointerSensor
  {
    pointerButton: PointerButton

    /*(!) private */ 
    clickable: /*(!) unknown */ Объект?

    hotPositionX: Число

    hotPositionY: Число

    clicking: /*(!) unknown */ Объект?

    clicked: /*(!) unknown */ Объект?

    clickX: Число

    clickY: Число

    /*(!) private */ 
    tryingDragging: ДаНет

    draggableData: /*(!) unknown */ Объект?

    dragSource: /*(!) unknown */ Объект?

    dragTarget: /*(!) unknown */ Объект?

    dragTargetWindow: /*(!) unknown */ Объект?

    previousDragTarget: /*(!) unknown */ Объект?

    dragStarted: ДаНет

    dragFinished: ДаНет

    startX: Число

    startY: Число

    /*(!) private */ 
    draggingData: /*(!) unknown */ Объект?

    dropAllowed: ДаНет

    draggingOver: ДаНет

    positionX: Число

    positionY: Число

    modifiers: KeyboardModifiers

    dropX: Число

    dropY: Число

    dropped: ДаНет

    immediatePositionX: Число

    immediatePositionY: Число

    immediateModifiers: KeyboardModifiers

    глобальный защищено
    DraggingThreshold: /*(!) any */ Объект?

    при создании(focusSensor: FocusSensor, windowSensor: WindowSensor)/*(!) any */ Объект?

    операция getData(): /*(!) unknown */ Объект?

    операция setData(value: /*(!) unknown */ Объект?)

    операция listen(element: HTMLElement?, enabled: ДаНет = да)

    скрыто типом 
    операция onPointerDown(e: PointerEvent)

    скрыто типом 
    операция onPointerMove(e: PointerEvent)

    скрыто типом 
    операция onPointerUp(e: PointerEvent)

    скрыто типом 
    операция onLostPointerCapture(e: PointerEvent)

    скрыто типом 
    операция onKeyDown(e: KeyboardEvent)

    скрыто типом 
    операция moveOver(e: PointerEvent)

    скрыто типом 
    операция tryClickingOrDragging(e: PointerEvent)

    скрыто типом 
    операция clickingOver(e: PointerEvent)

    скрыто типом 
    операция click(e: PointerEvent)

    скрыто типом 
    операция startDragging(e: PointerEvent)

    скрыто типом 
    операция dragOver(e: PointerEvent)

    скрыто типом 
    операция drop(e: PointerEvent)

    скрыто типом 
    операция finishDragging()

    скрыто типом 
    операция cancelDragging()

    скрыто типом 
    операция reset()

    скрыто типом 
    операция updateClicking(e: PointerEvent): ДаНет

    скрыто типом 
    операция updateDragTarget(e: PointerEvent)

    скрыто типом 
    операция whenClickingOrDragging()

    скрыто типом 
    операция whenMoving()
  }

}
