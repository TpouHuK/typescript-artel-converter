внешнее {
  //// The below copyright notice and the license permission notice
  //// shall be included in all copies or substantial portions.
  //// Copyright (C) 2019-2022 Nezaboodka Software <contact@nezaboodka.com>
  //// License: https://raw.githubusercontent.com/nezaboodka/verstak/master/LICENSE
  //// By contributing, you agree that your contributions will be
  //// automatically licensed under the license referred above.
  тип PointerSensor = объект на основе BasePointerSensor
  {
    pointerButton: PointerButton

    /*(!) private */ 
    clickable: /*(!) unknown */ Объект

    hotPositionX: Число

    hotPositionY: Число

    clicking: /*(!) unknown */ Объект

    clicked: /*(!) unknown */ Объект

    clickX: Число

    clickY: Число

    /*(!) private */ 
    tryingDragging: ДаНет

    draggableData: /*(!) unknown */ Объект

    dragSource: /*(!) unknown */ Объект

    dragTarget: /*(!) unknown */ Объект

    dragTargetWindow: /*(!) unknown */ Объект

    previousDragTarget: /*(!) unknown */ Объект

    dragStarted: ДаНет

    dragFinished: ДаНет

    startX: Число

    startY: Число

    /*(!) private */ 
    draggingData: /*(!) unknown */ Объект

    dropAllowed: ДаНет

    draggingOver: ДаНет

    positionX: Число

    positionY: Число

    modifiers: KeyboardModifiers

    dropX: Число

    dropY: Число

    dropped: ДаНет

    immediatePositionX: Число

    immediatePositionY: Число

    immediateModifiers: KeyboardModifiers

    глобальный защищено
    DraggingThreshold: /*(!) any */ Объект

    при создании(focusSensor: FocusSensor, windowSensor: WindowSensor)

    операция getData(): /*(!) unknown */ Объект

    операция setData(value: /*(!) unknown */ Объект): Ничего

    операция listen(element: HTMLElement?, enabled: ДаНет = да): Ничего

    скрыто типом 
    операция onPointerDown(e: PointerEvent): Ничего

    скрыто типом 
    операция onPointerMove(e: PointerEvent): Ничего

    скрыто типом 
    операция onPointerUp(e: PointerEvent): Ничего

    скрыто типом 
    операция onLostPointerCapture(e: PointerEvent): Ничего

    скрыто типом 
    операция onKeyDown(e: KeyboardEvent): Ничего

    скрыто типом 
    операция moveOver(e: PointerEvent): Ничего

    скрыто типом 
    операция tryClickingOrDragging(e: PointerEvent): Ничего

    скрыто типом 
    операция clickingOver(e: PointerEvent): Ничего

    скрыто типом 
    операция click(e: PointerEvent): Ничего

    скрыто типом 
    операция startDragging(e: PointerEvent): Ничего

    скрыто типом 
    операция dragOver(e: PointerEvent): Ничего

    скрыто типом 
    операция drop(e: PointerEvent): Ничего

    скрыто типом 
    операция finishDragging(): Ничего

    скрыто типом 
    операция cancelDragging(): Ничего

    скрыто типом 
    операция reset(): Ничего

    скрыто типом 
    операция updateClicking(e: PointerEvent): ДаНет

    скрыто типом 
    операция updateDragTarget(e: PointerEvent): Ничего

    скрыто типом 
    операция whenClickingOrDragging(): Ничего

    скрыто типом 
    операция whenMoving(): Ничего
  }

}
